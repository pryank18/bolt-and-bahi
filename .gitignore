# Dependencies
node_modules/

# Build output
dist/
build/

# Environment variables — NEVER commit real credentials.
# Only .env.example (with placeholder values) should be tracked.
.env
.env.local
.env.*.local
*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db

# Supabase CLI local state (if the Supabase CLI is used later for local dev)
.supabase/
